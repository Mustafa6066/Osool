# Osool Backend Dependencies
# Python 3.10+ required

# Web Framework
fastapi>=0.104.0
uvicorn>=0.24.0
gunicorn>=21.2.0  # Production WSGI server
pydantic>=2.5.0
email-validator>=2.0.0  # Required for Pydantic EmailStr validation

# AI / Machine Learning
openai>=1.0.0
anthropic>=0.39.0  # Claude AI integration for advanced reasoning
scikit-learn>=1.3.0
pandas>=2.1.0
joblib>=1.3.0
xgboost>=2.0.0
celery>=5.3.0
redis>=5.0.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.25.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
pgvector>=0.2.0
python-multipart>=0.0.6
alembic>=1.13.0

# Testing (Phase 4.2: Comprehensive Test Suite)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0  # Coverage reporting
pytest-mock>=3.12.0  # Mocking fixtures
pytest-xdist>=3.5.0  # Parallel test execution

# Database & AI (New)
supabase>=2.0.0
langchain>=0.1.0,<0.2.0  # Pin to 0.1.x to avoid breaking import changes
langchain-openai>=0.0.1,<0.2.0
langchain-community>=0.0.1,<0.2.0  # Required for SupabaseVectorStore
langchain-core>=0.1.0,<0.2.0  # Core functionality
langchain-anthropic>=0.1.0,<0.2.0  # Claude integration for LangChain
psycopg2-binary>=2.9.0

# Security hardening
bleach==6.1.0  # XSS protection via HTML sanitization
cryptography>=42.0.0  # Security updates
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1
slowapi>=0.1.9
authlib>=1.3.0

# Communication
twilio>=8.10.0
sendgrid>=6.11.0

# Monitoring & Error Tracking
sentry-sdk[fastapi]>=1.40.0  # Error tracking with FastAPI integration
prometheus-client>=0.19.0

# Scraping
beautifulsoup4>=4.12.0
requests>=2.31.0
playwright>=1.40.0
playwright-stealth>=1.0.6  # Anti-bot fingerprint patching for Playwright

# Async Task Queue (ingestion pipeline — decoupled from FastAPI process)
arq>=0.25.0  # Async Redis Queue: native asyncio worker, replaces APScheduler for property ingestion

# Cache
redis>=5.0.0

# Resilience & Retry
tenacity>=8.2.0

# Scheduling (weekly scrapers)
apscheduler>=3.10.0

# Structured Outputs
pydantic>=2.5.0

# S3-compatible object storage (image mirroring)
boto3>=1.34.0

# FRED economic data API
fredapi>=0.5.0

# RSS feed parsing
feedparser>=6.0.0
